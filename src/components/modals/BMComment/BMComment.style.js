import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: opa => ({
    backgroundColor: opa.set3,
    borderRadius: 5,
    padding: 10,
    margin: 5,
  }),
  buttons: {
    flexDirection: 'row',
  },
});
