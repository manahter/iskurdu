import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    position: 'absolute',
    justifyContent: 'center',
  },
  button: {
    width: 200,
    height: 48,
  },
});
